services:
  postgres:
    image: postgres:16
    container_name: ${CONTAINER_PREFIX:-qivro}-postgres
    restart: unless-stopped
    env_file:
      - ${ENV_FILE:-.env.production}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - qivro_internal

  ai:
    build:
      context: ../real-estate-ai
      dockerfile: Dockerfile
    image: qivro-ai:latest
    container_name: ${CONTAINER_PREFIX:-qivro}-ai
    restart: unless-stopped
    env_file:
      - ${ENV_FILE:-.env.production}
    expose:
      - "8000"
    networks:
      - qivro_internal

  backend:
    build:
      context: ../real-estate-backend
      dockerfile: Dockerfile
    image: qivro-backend:latest
    container_name: ${CONTAINER_PREFIX:-qivro}-backend
    restart: unless-stopped
    depends_on:
      - postgres
      - ai
    env_file:
      - ${ENV_FILE:-.env.production}
    environment:
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      FRONTEND_URL: ${FRONTEND_URL}
      ADMIN_FRONTEND_URL: ${ADMIN_FRONTEND_URL}
      CORS_ORIGINS: ${CORS_ORIGINS}
      AI_SERVICE_URL: http://ai:8000
    expose:
      - "3001"
    networks:
      - qivro_internal

  frontend:
    build:
      context: ../real-estate-ui
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: http://backend:3001/api
        NEXT_PUBLIC_BROWSER_API_URL: ${NEXT_PUBLIC_BROWSER_API_URL}
        NEXT_PUBLIC_GA_MEASUREMENT_ID: ${NEXT_PUBLIC_GA_MEASUREMENT_ID}
        NEXT_PUBLIC_POSTHOG_KEY: ${NEXT_PUBLIC_POSTHOG_KEY}
        NEXT_PUBLIC_POSTHOG_HOST: ${NEXT_PUBLIC_POSTHOG_HOST}
    image: qivro-frontend:latest
    container_name: ${CONTAINER_PREFIX:-qivro}-frontend
    restart: unless-stopped
    depends_on:
      - backend
    env_file:
      - ${ENV_FILE:-.env.production}
    expose:
      - "3000"
    networks:
      - qivro_internal

  admin:
    build:
      context: ../real-estate-ui
      dockerfile: apps/admin/Dockerfile
      args:
        NEXT_PUBLIC_API_URL: http://backend:3001/api
        NEXT_PUBLIC_BROWSER_API_URL: ${NEXT_PUBLIC_BROWSER_API_URL}
    image: qivro-admin:latest
    container_name: ${CONTAINER_PREFIX:-qivro}-admin
    restart: unless-stopped
    depends_on:
      - backend
    env_file:
      - ${ENV_FILE:-.env.production}
    expose:
      - "3002"
    networks:
      - qivro_internal

  nginx:
    image: nginx:1.27-alpine
    container_name: ${CONTAINER_PREFIX:-qivro}-nginx
    restart: unless-stopped
    depends_on:
      - frontend
      - admin
      - backend
    env_file:
      - ${ENV_FILE:-.env.production}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/templates:/etc/nginx/templates:ro
      - nginx_logs:/var/log/nginx
      - certbot_www:/var/www/certbot
      - certbot_conf:/etc/letsencrypt
    networks:
      - qivro_internal

  certbot:
    image: certbot/certbot
    container_name: ${CONTAINER_PREFIX:-qivro}-certbot
    profiles:
      - ssl
    volumes:
      - certbot_www:/var/www/certbot
      - certbot_conf:/etc/letsencrypt

  backup:
    image: postgres:16
    container_name: ${CONTAINER_PREFIX:-qivro}-db-backup
    restart: unless-stopped
    depends_on:
      - postgres
    env_file:
      - ${ENV_FILE:-.env.production}
    volumes:
      - ./backups:/backups
    command: >
      sh -c 'while true; do
        ts=$$(date +%Y%m%d-%H%M%S);
        PGPASSWORD=$$POSTGRES_PASSWORD pg_dump -h postgres -U $$POSTGRES_USER $$POSTGRES_DB | gzip > /backups/qivro-$$ts.sql.gz;
        find /backups -name "qivro-*.sql.gz" -mtime +$${BACKUP_RETENTION_DAYS:-14} -delete;
        sleep $${BACKUP_INTERVAL_SECONDS:-86400};
      done'
    networks:
      - qivro_internal

networks:
  qivro_internal:
    driver: bridge

volumes:
  postgres_data:
  nginx_logs:
  certbot_www:
  certbot_conf:
